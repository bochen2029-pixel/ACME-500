# Receipt · ACME-500 v2 step D · the tape becomes a store · 2026-09-14

*Claude Opus 5. Implements step D as planned in `docs/ARCHITECTURE_v2.md` §14b, in four sub-steps, each landed with its own battery, twenty lie arms, the tool oracles, the three modes diffed against C1, a commit and a push. Acceptance for the whole step: a kill survives (O18), and every reading identical to C1 under `live`. Branch `v2` of `C:\55555\acme500` (`github.com/bochen2029-pixel/ACME-500`). Built and run under WSL from a fresh copy; raw outputs under `receipts/step-d-2026-09-14/<sub-step>/`.*

## D0 · rows on change; the TICK-driven loop

**What changed.** A person's hold (reasons 1 blocked and 2 no attention) is written when the hold in force changes — the seat or the reason — and not per seat per day; any touch of the cell (a fetch, an act, a mid-case hold, a handoff, a decision, an effect) ends the hold in force, so the next hold is a change and a row. The resident's hold likewise on a change of (reason, band), ended by an effect or an undo. A `STRATUM` row is written when the draw in force changes (kind, canary rate or audit rate) and stands until then. The hold in force and the draw in force are folded fields (`Obligation::hold_reason/hold_seat/mhold_reason/mhold_band`; the ledger's per-cell stratum kind and both rates), compared by O14. The meters count what they counted before (`holds recorded`, `held (a row, with its margin)` are per cell-day, row or not), so no reading moves. Reason-3 holds, a day that ends mid-case, carry state and stay per occurrence.

**The loop is the world port's.** Intake lands (`ARRIVE` rows), a `TICK` is folded (`tick_fold`: the row is written and the ledger's day moves with it), the governor draws, the resident runs its period **on the day the ledger says** — `Resident::period` and `grade` no longer take a day; the governor's `draw_strata` reads the ledger's — then the incumbent works, the world settles, the governor folds the outcomes and steps the ladder. The `TICK` row's value is wall seconds since the last tick, written 0 by the sim and never read by the machine; the dynamic O25 (D3) varies it.

**Acceptance.** Build 0 warnings. `--selftest` **22 of 22** (19.2 s); lie arms 0–19 each exit 0 with the lied-to oracle's own line PASS; O17 both lies, O28 (18 functions), O25a green. **Every reading identical to C1** in `--sim`, `--automate` and `--twin`: the only lines that differ are the tape line, the row histogram and the wall times. O14 holds across the change (58,367 cells, 0 field diffs, the hold in force and the draw in force included); O21 unchanged at χ² 72.2 on 72 cells (the draw in force is read from its row until the next).

**What the tape lost.**

| run | rows, C1 | rows, D0 | HOLD | STRATUM | chain head |
|---|---|---|---|---|---|
| `--sim` | 5,673,669 | **1,607,033** (−72%) | 4,150,074 → 83,438 | — | `3f9a4a26fab68e5c` → `01fa6bcaa35c647c` |
| `--automate` | 4,543,740 | **2,179,225** (−52%) | 2,390,894 → 124,301 | 289,062 → 191,140 | — |
| the battery world | 460,153 | 402,660 | | | |

`PROPOSAL` (138,345), `ACT`, `CONTEXT`, `ARRIVE`, `DECIDE`, `EFFECT`, `ESCALATE`, `OUTCOME` counts are unchanged, as they must be: nothing that carries state was touched. The `STRATUM` saving is smaller than the hold's because the audit rate moves with the class's outcome count every period a wager outcome lands, and the row follows the rate; a rate quantised to the term (step E, with the retained stratum) would make it a row per cell per term.

**A semantic the fold now has to know.** Before D0 a `HOLD` row meant "held today"; after it, a hold stands from its row until the next row about the cell. A fold that wants cell-days held integrates between rows; `minutes_by_via` and the row histogram are unaffected, and no reading in the three modes was ever a hold-row count.

Tree 4,608 lines. Hashes (sha256, first 16): human.h `87f98823fed31429` · ledger.h `905b50496e2bab62` · governor.h `c55d0b7d1c7bfa9b` · solver.h `0a51ac6d9be5e3a4` · machine.h `771c7703ba4ab250` · main.cpp `93e42beeb4c904f7`; core.h, firm.h, world.h, port.h, license.h, report.h, CMakeLists.txt as at C1. Outputs: `receipts/step-d-2026-09-14/d0/` (26 files).

## D1 · the durable tape, the checkpoint, `--dump`; O18

**The file** (`include/acme/tapefile.h`, kernel-side, scanned by O25a). A tape is a directory of segments, one million records each, every record the 40 bytes `Rec` occupies in memory (packed; `static_assert 40`), so the wire format is the struct and `memcmp` stays the theorem. A segment opens with a packed 64-byte header — magic, record version, the alphabet pin, its index, the row index of its first record, and the chain head at its start — so a reader recovers the head by re-walking one segment and checks each segment's header against the running head. The tape sees every record the moment it is chained (`Tape::sink`). A torn tail (a last segment whose length is not header + k × 40) is truncated to the last whole record and the bytes are reported; complete bytes whose head does not match the next header are corruption and the open refuses. The sidecar `tape.meta.json` carries the pins, the judge hashes, the rows, the head and the cursor, rewritten by atomic rename.

**The checkpoint** (`include/acme/checkpoint.h`, main-side). At the close of every tenth live period (`--ckpt-every`), `ckpt/NNNNNN.state` holds everything a future row depends on that the seed does not regenerate: the ledger (cells, index, open set, day, memo, strata, meter), the incumbent's meter, the seats' live state and the writ, the resident (field with its Adam state, hand, stats, supervision, memo hashes, the compiled table), the governor (salt, ladder, rates), the two judges' learned competence, the replay's counts, and the tape's cursor and head; `NNNNNN.meta` — day, cursor, chain head, the BLAKE2b of the state file — is written **last, by rename**. A state without its meta was in flight when the process died and a restore skips it.

**The restore** (`--resume`). Load the segments and re-walk the chain; take the newest checkpoint whose meta exists, whose state digests to what the meta says and whose head is the tape's head at its cursor; **cut the tape back to the cursor** in memory and on disk — the rows of the periods in flight were uncommitted (in a real lane they come back from the source's own cursor; in the sim the plant regenerates them) — put a `NOTE` row on the tape (code 1; the rows and the torn bytes dropped; `RF_TORN_OR_SURVEY`), and run the world port's loop from the day after. A `NOTE` is a row of no period: the next `TICK` does not count it, which is what makes the continued tape equal the unkilled one row for row.

**O18 · a kill survives** (lie arm 20). The battery runs the short automate to a durable tape with a checkpoint every ten periods, then leaves the files as a dying process would: the last segment cut 1,000 rows and 17 bytes past checkpoint 59's cursor, checkpoint 59's meta missing (in flight), checkpoint 69 never written. The restore must find 49. Reading:

```
  [PASS] O18  ... (cut at row 352711 + 17 bytes with checkpoint 59 in flight; restored at day 49, 60069 rows dropped;
               continued run: 402661 rows, chain verified, 0 fold diffs, rows equal the unkilled run's)
```

402,661 = the unkilled run's 402,660 + one `NOTE`. The lie is a restore that keeps the rows of the periods in flight and re-runs them on top: 462,730 rows, one fold diff (a cell opened twice), rows differing from row 352,711 — caught. (The spec's lie, a torn record taken as a row, is inert under the cursor rule — everything past the cursor is dropped whatever its shape — so the lie moved to the rule that actually protects the tape.)

**A real kill.** `timeout -s KILL 30 ./acme --automate --tape TK` at 500 seats: the process died with 1,842,657 rows on disk, a 32-byte torn record, checkpoints 189 and 199 written. `--automate --tape TK --resume --dump DUMPK`: restored at day 199 (cursor 1,781,013), 61,644 rows of the periods in flight dropped, continued to day 260 in 18.8 s: 2,179,226 rows, chain verified, and **the rows equal the unkilled run's dump row for row, the one `NOTE` aside** (`kill_resume_check.txt`: sha256 unkilled `212244f5be5ccf0e`, resumed `7ae0b415464af8b5`, first differing row none).

**The durable run moves no reading.** `--automate --tape T --dump DUMP` at 500 seats: stdout identical to the plain automate outside the timing lines; three segments (40,000,064 + 40,000,064 + 7,169,064 bytes = 2,179,225 records), eight checkpoints of 22.6–29.2 MB, `tape.meta.json` with head `d24d17f399c74b19`; the dump's `tape.bin` (87,169,000 bytes) is byte-identical to the segment bodies concatenated (`tape_bin_check.txt`).

**`--dump DIR`** (`include/acme/dump.h`). The observer lane's dump patch, taken into the mainline as its own header and wired at the day loops: `tape.bin`, `tape.meta.json`, `firm.json`, `phases.json`, `snapshots.jsonl` with the fields their `dump.h` wrote, plus `commit` from `-DACME_COMMIT`. Their patch retires; their page proves the format. A resumed run's `snapshots.jsonl` starts at the restore (the snapshots are the page's convenience, not part of the tape).

**Two prints, as added lines (§9.3).** *Cognitive path length*, per class, cells a person decided against cells the machine disposed of — rows from arrival to disposition, seats passed, days (means), from the automate: all classes, person **12.0 rows · 1.11 seats · 22.4 days** against machine **6.6 · 0.38 · 41.1**. The machine's path is shorter in rows and in seats on every class (`claim.settle.auth`: 56.7 rows and 5.0 seats through the warrant chain against 9.3 and 0.84; `kyc.review` 15.0 against 4.6) and **longer in days on most**, because the machine takes the cells people never reached: the days column is the backlog's age, not the machine's latency, and the print says so by putting both arms beside each other. F-PATH holds on every licensed class. *The frontier bill by value*: 78,869 rentals; 61 percent on classes worth $9k–$20k, **0.8 percent on the five classes worth $60k–$150k** — the rented mind is spent on volume, not on consequence, which is the E lever (rank the frontier's queue by margin thinness × value) with its baseline.

**Acceptance.** Build 0 warnings. `--selftest` **23 of 23** (22.3 s); lie arms 0–20 each exit 0 with the lied-to oracle's own line PASS; O17 both lies, O28 (18 functions), O25a over `tapefile.h` too. Every reading in `--sim`, `--automate`, `--twin` identical to D0; the lines that differ are the tape line, the wall times and the added prints. Tree 5,561 lines. Hashes (sha256, first 16): core.h `3be572ae2180d116` · report.h `db89f9f351dbc77d` · tapefile.h `e347a30c9d4caada` · checkpoint.h `81b4c93131a059e4` · dump.h `81579d4a7619e9a4` · main.cpp `089624b063b23b1a` · CMakeLists.txt `b6444f5c44e23fae`; the rest as at D0. Outputs: `receipts/step-d-2026-09-14/d1/` (40 files).

**Not in D1.** The sim's own resume (`--sim --tape` writes the tape and its meta, and checkpoints only the machine run); a checkpoint of a run whose judge is `--judge null|rules` (the stubs hold no state and are not checkpointed); the dump's snapshots across a restore; segment-level verification on open is the header chain, and the full walk from genesis is what `--resume` and O18 do.
